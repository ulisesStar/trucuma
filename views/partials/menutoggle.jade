md-button.md-button-toggle(ng-click="toggle()" ui-sref="{{section.state}}" aria-controls="docs-menu-{{section.name | nospace}}" flex layout="row" aria-expanded="{{isOpen()}}")
      md-icon.material-icons(style="margin:0px") {{section.icon}}
      | {{section.name}}
      span(aria-hidden="true" class="pull-right fa fa-chevron-down md-toggle-icon" ng-class="{toggled : isOpen()}")
ul(ng-show="isOpen()" id="docs-menu-{{section.name | nospace}}" class="menu-toggle-list")
      li(ng-repeat="page in section.pages")
            menu-link(section="page")